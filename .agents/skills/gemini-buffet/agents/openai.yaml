interface:
  display_name: "Gemini Buffet"
  short_description: "Repo-specific patterns and workflows for gemini-buffet"
  default_prompt: "Use the gemini-buffet repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true